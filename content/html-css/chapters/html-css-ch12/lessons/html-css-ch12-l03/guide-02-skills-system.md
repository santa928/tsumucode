## Guide 2 — Skills、Works、再利用Class

1. Skillsを`ul`と`li`で表す
2. 3つ以上のSkillを置く
3. AboutとSkillsへ同じSurface Classを使う
4. Section固有の違いは追加Classで表す
5. Worksは`article`を2件以上置き、親へ`display: grid`を指定する

Class名は見た目の色ではなく、役割が伝わる名前にします。工程3では2列のGridを作り、狭い画面への対応は次の工程で行います。
