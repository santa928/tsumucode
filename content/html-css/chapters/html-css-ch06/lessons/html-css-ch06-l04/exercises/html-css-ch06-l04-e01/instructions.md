## 2枚のCardへBase Styleを再利用する

1. `data-card`を持つCardを2枚用意します。
2. 共通のBase Classへ白いBackgroundと24pxのPaddingを宣言します。
3. 同じBase Classを両方のCardへ適用します。
4. 2枚とも同じComputed BackgroundとPaddingを持つことを確認します。

TextやClass名の完全一致ではなく、2枚へ届いた最終表示で判定します。
