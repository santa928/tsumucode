## Base ClassとModifier Classを組み合わせる

CSSは完成済みです。`index.html`のClass属性を2箇所だけ変更します。

1. 1枚目の`class="html-card"`を`class="card"`へ変えます。
2. 2枚目の`class="css-card"`を`class="card card--featured"`へ変えます。

実行後に、両方の共通Styleと2枚目だけの左Borderを確認します。
