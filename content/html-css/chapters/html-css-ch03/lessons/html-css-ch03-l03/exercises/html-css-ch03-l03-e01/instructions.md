## LabelとButton Typeを整える

Sandbox内のProfile Formを、目的が伝わり誤送信しない構造へ直します。

1. labelのforをnicknameから、inputのidと同じdisplay-nameへ変更します。
2. LabelをClickしてInputへFocusできる関連付けを作ります。
3. 「入力を確認する」Buttonへtype="button"を追加します。
4. Previewと判定でLabel、Control、Buttonを確認します。

この実習では入力内容を外部へ送りません。Form送信はPreview Sandboxで無効です。
