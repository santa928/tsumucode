## Grid GalleryとFlex Actionを組み合わせる

1. 幅600pxの`[data-gallery]`を2 ColumnのGridにします。
2. Feature Cardを2 Track分の全幅へ広げます。
3. Card内の`[data-actions]`はFlexboxで横1列へ並べます。
4. Feature CardのWidthが600pxで、Galleryに横Overflowがないことを確認します。

外側の2次元配置と内側の1次元配置を別々のContainerへ任せます。
