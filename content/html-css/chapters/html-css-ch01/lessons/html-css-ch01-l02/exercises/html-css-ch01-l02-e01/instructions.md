## pをmainのChildへ移す

`main`の外にある既存の`p`を、`h1`と同じParentのChildへ移します。

1. `main`の終了Tagと、その後ろにある`p`の1行を探します。
2. `p`の1行を切り取り、`h1`の次かつ`main`の終了Tagより前へ貼り付けます。
3. `p`の行頭を`h1`と同じ半角Space 2文字ぶん右へそろえます。
4. Previewを見てから、親子関係を判定します。

`main`や`p`のTagを新しく暗記して書く課題ではありません。用意された1行を、図と同じ親子関係へ移します。CSSには触りません。
