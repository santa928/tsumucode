## 高さの違うItemをCross Axisへ揃える

1. `[data-align]`の`align-items: flex-start;`を`center`へ変えます。
2. `[data-end]`の`align-self: center;`を`flex-end`へ変えます。
3. Previewで高さ60pxのItemがy 102pxへあることを確かめます。
4. 例外の高さ40pxのItemがy 192pxへあることも確かめます。

Container上端はBody Paddingによるy 32pxです。手が止まったら「前のスライド」で中央線と例外の図を見直せます。
