## 読めて意味が伝わる公開状態を作る

1. `[data-status]`のTextとBackgroundを4.5:1以上の組み合わせにします。
2. 色の丸だけに頼らず、「公開中」などのTextへ`data-status-text`を付けます。
3. Previewで色を隠しても状態が分かるか確かめます。
4. 判定結果でContrastと状態Textを別々に確認します。

色や状態の文言は、2つの観測条件を満たす別案でも合格します。
