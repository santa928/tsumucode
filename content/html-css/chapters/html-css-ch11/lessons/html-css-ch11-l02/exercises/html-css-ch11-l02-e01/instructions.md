## labelとaltで2種類の名前を仕上げる

`index.html`の未完成な2箇所だけを変更します。

1. 「表示名」を包む`field-caption`の`span`を、`for="display-name"`を持つ`label`へ置き換えます。
2. `data-avatar`画像の空の`alt`へ、画像内容が分かる短い説明を書きます。
3. 完成済みの「つむのHTML/CSS作品を見る」というLink Textは残します。
4. 判定結果でInput、画像、Linkの名前を別々に確認します。

画像の説明文は、空でなく内容が分かれば別案でも合格します。
